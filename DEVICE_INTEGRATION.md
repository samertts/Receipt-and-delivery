# تكامل الأجهزة الطرفية

يوفر النظام طبقة **web-first** لتقليل تثبيت البرامج المحلية، مع إبقاء الوصول إلى الأجهزة الحساسة اختياريًا ومقيدًا. يمكن فتح صفحة **مركز الأجهزة** من القائمة الجانبية عبر المسار `/devices` للمستخدمين الذين يملكون صلاحية `use_devices`.

> لا يستطيع المتصفح التحكم صامتًا في كل أجهزة USB أو الطابعات الحرارية أو المنافذ التسلسلية. الميزات التالية تعمل بعد موافقة المستخدم وبحسب دعم المتصفح ونظام التشغيل.

## الميزات المتاحة

| الوظيفة | التنفيذ | المتطلبات والقيود |
|---|---|---|
| الكاميرا والباركود | `getUserMedia` مع ZXing | يلزم HTTPS أو `localhost` وإذن الكاميرا. يفضل Chromium، وقد يختلف اختيار الكاميرا بين الأجهزة. |
| قارئ باركود USB | Keyboard-wedge | يعمل القارئ كلوحة مفاتيح ويرسل الرمز ثم Enter؛ لا يحتاج تعريفًا خاصًا، ويجب إبقاء صفحة النظام نشطة. |
| OCR | Tesseract.js داخل المتصفح | يبدأ صراحة من زر المستخدم، ويدعم الإنجليزية والعربية عبر `eng+ara`. قد يستغرق أول تشغيل وقتًا لتنزيل بيانات اللغة؛ النص الناتج قابل للتحرير ولا يغير بيانات المعاملة تلقائيًا. |
| NFC | Web NFC / `NDEFReader` | اختياري، وغالبًا يتوفر على Android Chromium فقط مع HTTPS. غير متاح عمومًا على أجهزة سطح المكتب وiOS. |
| الطباعة | نافذة الطباعة الأصلية | تستخدم `window.print()` وتترك اختيار الطابعة والحجم للمستخدم. تصلح للطابعات النظامية دون تحكم خام في USB. |
| طابعات حرارية وUSB/Serial | جسر محلي اختياري | يحتاج برنامجًا محليًا موثوقًا ومقيدًا على الجهاز نفسه؛ لا توجد خدمة مستضافة يمكنها الوصول إلى طابعة المستخدم المحلية. |

## الخصوصية والأمان

تتم معالجة OCR في المتصفح بعد اختيار الصورة صراحة، ولا يرفع النظام الصورة إلى الخادم ضمن هذه المرحلة. يجب ألا تعتمد النتائج آليًا في حقول ذات أثر قانوني؛ راجع النص وعدله قبل نسخه إلى معاملة.

يستخدم التطبيق صلاحية RBAC باسم `use_devices` لعرض مركز الأجهزة. أما أي جسر محلي مستقبلي فيجب أن يستمع إلى `127.0.0.1` فقط، ويستخدم رمز اقتران أو ترويسة سرية لكل تثبيت، ويقبل `Origin` من قائمة مضبوطة، ويعرض `/health` للفحص دون كشف بيانات حساسة. لا ينبغي توفير نقطة طباعة خام غير موثقة أو غير مصادق عليها.

## خطوات التشغيل

شغّل الواجهة عبر HTTPS في الإنتاج أو عبر `localhost` أثناء التطوير. افتح `/devices`، امنح إذن الكاميرا عند الطلب، ثم استخدم قارئ الباركود أمام الكاميرا أو اترك قارئ USB يرسل الرمز إلى الحقل النشط. اختر صورة من زر OCR لتشغيل التعرف يدويًا. وللقراءة عبر NFC، استخدم جهازًا ومتصفحًا يدعمان Web NFC واضغط الزر ثم قرّب البطاقة.

الطباعة الحالية لا تحتاج تثبيتًا إضافيًا: اضغط **طباعة اختبار** ثم اختر الطابعة من حوار النظام. يمكن لاحقًا إضافة جسر محلي مستقل للطابعات الحرارية، لكن تشغيله سيكون مسؤولية مدير الجهاز وليس الخادم السحابي.

## مراجع التنفيذ

[1] [ZXing browser](https://github.com/zxing-js/browser) — قارئ باركود للمتصفح.

[2] [Tesseract.js](https://github.com/naptha/tesseract.js) — OCR JavaScript متعدد اللغات.

[3] [MDN: MediaDevices.getUserMedia](https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getUserMedia) — صلاحيات الكاميرا والسياق الآمن.

[4] [MDN: Web NFC](https://developer.mozilla.org/en-US/docs/Web/API/Web_NFC_API) — دعم NDEFReader وحدود التوافق.
