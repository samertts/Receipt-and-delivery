# تقرير فحص الأخطاء والمشاكل ونقاط الضعف

**التاريخ:** 2026-07-06  
**النطاق:** فحص سريع ثابت ومستند إلى الأدلة لمستودع `Receipt-and-delivery`، مع تشغيل فحوصات `pytest` و`ruff` واستعراض نقاط الأمن والجودة والالتزام الدستوري.

## ملخص تنفيذي

تم العثور على عدة مخاطر تحتاج معالجة قبل أي اعتماد إنتاجي جديد. أهمها:

- **فشل اختبارات المشروع الإلزامية** بسبب نقص تبعية `bcrypt` في البيئة الحالية، ما يمنع إثبات السلامة التشغيلية وفق متطلبات المستودع.
- **تخزين رموز الوصول والتحديث في `localStorage`** في الواجهة، وهو خطر مرتفع عند حدوث XSS.
- **نظام تحديد معدل الطلبات قابل للتجاوز** لأنه يعتمد افتراضياً على الذاكرة ويقبل تعطيله بمتغير بيئة.
- **مفتاح JWT قد يُولّد ويُحفظ تلقائياً داخل شجرة التطبيق** عند غياب `SECRET_KEY`، ما يخالف مبدأ الإعداد الآمن افتراضياً في البيئات الإنتاجية.
- **التحقق من صلاحية رموز JWT لا يتحقق من نوع الرمز أو القائمة السوداء** في اعتماد المستخدم الحالي، ما قد يسمح باستخدام refresh token كرمز وصول أو استمرار صلاحية رموز مسجلة خروجاً في مسارات محمية.
- **مسار المرفقات يتعامل مباشرة مع قاعدة البيانات والملفات داخل طبقة API** بدلاً من خدمة تطبيقية/مستودع، ما يخالف اتجاه حدود الخدمة الموثق.
- **خدمات سطح المكتب تحتوي وصولاً مباشراً واسعاً للقاعدة من طبقة الخدمات**، وهو ضعف معماري مقابل مسار التنفيذ المعتمد في `AGENTS.md`.

## منهجية الفحص والأدلة

الأوامر التي تم تنفيذها:

| الأمر | النتيجة |
|---|---|
| `python3 -m ruff check .` | نجح: `All checks passed!` |
| `python3 -m pytest tests/ -q` | فشل أثناء جمع الاختبارات بسبب `ModuleNotFoundError: No module named 'bcrypt'` |
| `python3 -m bandit -r lab_system backend unified_platform scripts -q` | لم يُنفّذ الفحص لأن `bandit` غير مثبت في البيئة |
| `rg ...` | استُخدم لاستخراج مؤشرات المخاطر مثل `localStorage`, `sqlite3.connect`, `TODO`, `RATE_LIMIT_DISABLED`, والاستعلامات المباشرة |

## النتائج التفصيلية

### 1. فشل الاختبارات الإلزامية بسبب نقص `bcrypt`

**الشدة:** عالية  
**المجال:** جودة / قابلية اعتماد / امتثال workflow  
**الدليل:** فشل `python3 -m pytest tests/ -q` أثناء جمع `tests/test_desktop_auth.py` لأن `lab_system/app/auth/security.py` يستورد `bcrypt`، والتبعية غير موجودة في البيئة.

**الأثر:**

- لا يمكن إثبات أن النظام يمر باختبارات الانحدار الحالية.
- يمنع الالتزام بمتطلبات `AGENTS.md` التي تشترط نجاح `python3 -m pytest tests/ -q` قبل الاعتماد.

**التوصية:**

- تثبيت التبعيات المطلوبة في ملف المتطلبات المناسب أو توحيد بيئة الاختبار.
- إضافة فحص CI يثبت تثبيت كل تبعيات سطح المكتب والـ backend قبل تشغيل الاختبارات.

---

### 2. تخزين رموز المصادقة في `localStorage`

**الشدة:** عالية  
**المجال:** أمن / جلسات المستخدم  
**الدليل:** الملف `frontend/src/stores/auth.js` يقرأ `access_token` و`refresh_token` من `localStorage` ويكتبها إليه. كما توجد ملاحظة داخلية تقر بأن ذلك عرضة لـ XSS وتطلب الانتقال إلى httpOnly cookies.

**الأثر:**

- أي ثغرة XSS في الواجهة قد تسمح بسرقة access token وrefresh token.
- وجود refresh token في JavaScript يزيد أثر الاختراق لأن المهاجم يستطيع تجديد الجلسة.

**التوصية:**

- نقل refresh token إلى cookie تحمل `HttpOnly`, `Secure`, `SameSite=Strict/Lax`.
- إبقاء access token قصير العمر في الذاكرة فقط إن أمكن.
- إضافة سياسة CSP واختبارات أمنية تؤكد عدم وجود tokens في Web Storage.

---

### 3. محدد معدل الطلبات غير كافٍ للإنتاج وقابل للتعطيل

**الشدة:** عالية  
**المجال:** أمن / مقاومة brute-force وDoS  
**الدليل:** `MemoryRateLimiter` يذكر أن الحالة تضيع عند إعادة التشغيل وأن Redis مجرد TODO/بديل. كما أن middleware يتجاوز تحديد المعدل بالكامل عند وجود متغير البيئة `RATE_LIMIT_DISABLED`.

**الأثر:**

- إعادة تشغيل الخدمة تصفر عدادات محاولات الدخول.
- في بيئة متعددة العمليات أو متعددة العقد، كل عملية لها عداد مستقل، ما يجعل الحماية قابلة للتجاوز.
- متغير بيئة واحد قد يعطل طبقة حماية مهمة دون fail-closed.

**التوصية:**

- جعل Redis/قاعدة بيانات متطلباً إجبارياً في الإنتاج.
- منع `RATE_LIMIT_DISABLED` في بيئات غير التطوير، أو جعله fail-closed مع تحذير يمنع التشغيل.
- ربط المعدل بمزيج من IP + اسم المستخدم + device fingerprint عند login.

---

### 4. سياسة مفتاح JWT غير آمنة افتراضياً للإنتاج

**الشدة:** عالية  
**المجال:** أمن / إدارة أسرار  
**الدليل:** عند غياب `secret_key` أو ضبطه على `change-me`، يتم توليد مفتاح وحفظه تلقائياً في ملف `.generated_secret_key.json` داخل مسار backend.

**الأثر:**

- قد تعمل بيئة إنتاج دون secret مُدار مركزياً أو مراجع أمنياً.
- الملف المحلي قد يتسرب عبر نسخ احتياطية أو نشر خاطئ.
- دوران الأسرار وإبطال الجلسات يصبح غير مضبوط.

**التوصية:**

- في الإنتاج، يجب أن يفشل التشغيل إذا لم يتم ضبط `SECRET_KEY` قوي عبر secret manager أو متغير بيئة آمن.
- وضع صلاحيات ملف صارمة عند اللجوء لتوليد محلي في التطوير فقط.
- إضافة اختبار يؤكد أن بيئة الإنتاج لا تقبل `secret_key` فارغاً أو `change-me`.

---

### 5. اعتماد المستخدم الحالي لا يتحقق من نوع الرمز ولا من blacklist

**الشدة:** عالية  
**المجال:** أمن / مصادقة  
**الدليل:** `get_current_user` يفك JWT ويتحقق من `sub` فقط، ولا يتحقق أن `type == "access"`، ولا يستعلم عن `BlacklistedToken` قبل قبول الرمز.

**الأثر:**

- refresh token صالح قد يستخدم كـ Bearer token للوصول إلى مسارات محمية إن كان يحتوي `sub` صالحاً.
- logout يضيف access token إلى blacklist، لكن المسارات المحمية لا تفحص blacklist، لذلك قد يبقى الرمز مقبولاً حتى انتهاء صلاحيته.

**التوصية:**

- فرض `payload["type"] == "access"` داخل `get_current_user`.
- فحص blacklist في كل اعتماد bearer token أو استخدام token version/session id في قاعدة البيانات.
- إضافة اختبارات أمنية: منع refresh token على endpoint محمي، ومنع access token بعد logout.

---

### 6. API المرفقات يتجاوز حدود الخدمة ويفتقر لعزل الصلاحيات على مستوى المورد

**الشدة:** متوسطة إلى عالية  
**المجال:** معمارية / أمن بيانات  
**الدليل:** `backend/app/api/v1/attachments.py` ينفذ من route مباشرة: استعلامات SQLAlchemy، إنشاء المجلد، كتابة الملف، إنشاء سجل المرفق، وcommit. كذلك تحميل المرفق يتطلب فقط `view_transactions` دون تحقق من نطاق المؤسسة/المعاملة للمستخدم.

**الأثر:**

- يخالف مسار `UI → Application Services → Domain Services → Repositories → Database`.
- يصعب تدقيق أحداث المرفقات مركزياً، ولا يظهر تسجيل audit في upload/download.
- مستخدم لديه صلاحية عامة `view_transactions` قد يستطيع تحميل مرفقات معاملات خارج نطاقه إذا عرف `attachment_id`.

**التوصية:**

- نقل منطق المرفقات إلى `AttachmentService` و`AttachmentRepository`.
- فرض authorization على مستوى المعاملة/المؤسسة/الموقع قبل upload/download.
- تسجيل أحداث audit مثل `attachment_uploaded` و`attachment_downloaded` مع hash وحجم الملف.

---

### 7. خدمات سطح المكتب تستخدم وصولاً مباشراً واسعاً لقاعدة البيانات خارج المستودعات

**الشدة:** متوسطة  
**المجال:** معمارية / قابلية الصيانة / امتثال  
**الدليل:** `lab_system/app/services/user_service.py` يستدعي `_db.get_conn()` وينفذ SQL مباشرة في عمليات مثل seed, authenticate, list_users, create_user, enable/disable/reset password.

**الأثر:**

- يضعف فرض سياسات audit/authorization/validation بشكل موحد.
- يصعب تبديل قاعدة البيانات أو توسيع النظام وطنياً دون إعادة عمل.
- يزيد احتمال اختلاف السلوك بين الخدمات والمستودعات.

**التوصية:**

- إنشاء `UserRepository` لسطح المكتب أو استخدام `BaseRepository` الموجود.
- إبقاء الخدمات مسؤولة عن منطق الأعمال فقط، ونقل SQL للمستودعات.
- إضافة اختبار معماري يمنع `_db.get_conn()` داخل `lab_system/app/services` إلا في استثناءات موثقة.

---

### 8. التحقق من قوة كلمة المرور غير موحد بين backend وسطح المكتب

**الشدة:** متوسطة  
**المجال:** أمن / اتساق سياسات الهوية  
**الدليل:** backend يحتوي `validate_password_strength`، لكن `lab_system/app/services/user_service.py` يحفظ كلمات مرور جديدة أو يعيد ضبطها دون استدعاء سياسة قوة كلمة المرور.

**الأثر:**

- يمكن إنشاء كلمات مرور ضعيفة في مسار سطح المكتب حتى إذا كان backend يرفضها.
- يخرق هدف "One Identity" و"Security by Default" من الدستور.

**التوصية:**

- استخراج سياسة كلمة المرور إلى وحدة مشتركة أو تطبيقها في خدمة سطح المكتب.
- إضافة اختبارات تمنع إنشاء/إعادة ضبط كلمة مرور ضعيفة عبر كل المسارات.

---

### 9. فحص static security المتخصص غير متاح حالياً

**الشدة:** متوسطة  
**المجال:** أدوات / ضمان جودة  
**الدليل:** محاولة تشغيل `python3 -m bandit -r ...` فشلت لأن الحزمة غير مثبتة.

**الأثر:**

- قد تبقى مشاكل مثل deserialization أو subprocess أو path traversal غير مكتشفة آلياً.

**التوصية:**

- إضافة `bandit` أو أداة SAST معتمدة إلى dependencies/CI.
- جعل تقرير SAST جزءاً من شهادة الإصدار.

## أولويات المعالجة المقترحة

1. **إصلاح بيئة الاختبار** حتى يمر `pytest` كاملاً، ثم جعل ذلك بوابة إلزامية في CI.
2. **إصلاح JWT/session handling**: منع refresh token على المسارات المحمية وفحص blacklist.
3. **نقل tokens من localStorage** إلى httpOnly cookies أو نمط آمن مكافئ.
4. **فرض secret configuration في الإنتاج** ومنع التشغيل بـ secret تلقائي.
5. **تقوية rate limiting** باستخدام Redis/DB في الإنتاج وإلغاء التعطيل المفتوح.
6. **إعادة هيكلة المرفقات وخدمات سطح المكتب** لاحترام حدود الخدمة وإضافة audit/authorization على مستوى المورد.
7. **توحيد سياسة كلمات المرور** بين backend وسطح المكتب.
8. **إضافة SAST رسمي** مثل Bandit إلى CI.

## حالة الاعتماد الحالية

بناءً على الأدلة أعلاه، **لا يُنصح باعتبار الفرع جاهزاً للإصدار الإنتاجي** حتى معالجة البنود عالية الشدة وتشغيل الاختبارات الإلزامية بنجاح.

## تحديث المعالجة الآمنة — 2026-07-06

تم تنفيذ حزمة إصلاحات آمنة لمعالجة المخاطر ذات الأولوية من التقرير:

- إزالة تخزين رموز الوصول والتحديث وبيانات المستخدم من `localStorage` في الواجهة واستبداله بمخزن جلسة داخل الذاكرة.
- تشديد اعتماد JWT في backend بحيث لا تُقبل إلا رموز `access`، مع رفض الرموز الموجودة في blacklist.
- منع تعطيل rate limiting في بيئات `prod`/`production`/`staging` وإرجاع خطأ واضح عند محاولة ذلك.
- فرض وجود `SECRET_KEY` صريح وآمن خارج بيئة التطوير بدلاً من قبول توليد تلقائي في الإنتاج.
- إضافة بديل آمن لتجزئة كلمات مرور سطح المكتب باستخدام PBKDF2-HMAC-SHA256 عند غياب `bcrypt`، مع الحفاظ على دعم bcrypt عند توفره.
- فرض سياسة قوة كلمة المرور في مسارات تغيير/إنشاء/إعادة ضبط كلمات مرور مستخدمي سطح المكتب.
- إضافة اختبارات انحدار ثابتة وأمنية للتأكد من عدم عودة تخزين tokens في `localStorage`، ومن وجود فحوصات JWT/blacklist/secret/rate-limit، ومن رفض كلمات المرور الضعيفة في مسارات سطح المكتب.

تبقى البنود المعمارية الأكبر، مثل نقل مرفقات API بالكامل إلى خدمة ومخازن مستقلة وعزل الصلاحيات على مستوى المورد، مرشحة كمرحلة تحسين لاحقة لأنها تتطلب إعادة تصميم أوسع واختبارات تكامل إضافية.
